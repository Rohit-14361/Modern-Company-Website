# TODO

- [x] Create `frontend/src/components/common/OurProcess.jsx` to render an “Our Process” section (Discover → Design → Develop → Deploy).
- [x] Render `OurProcess` above the existing `Footer` on the Home page.

- [x] Keep styling consistent with existing footer (dark gray background, appropriate Tailwind classes).
- [ ] Run frontend lint/build (if available) to ensure no syntax errors.


